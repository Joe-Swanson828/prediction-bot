# ============================================================
# Prediction Market Trading Bot — Environment Variables
# ============================================================
# Copy this file to .env and fill in your actual values.
# NEVER commit .env to version control.
# All values are optional for paper trading mode.
# ============================================================

# === TRADING MODE ===
TRADING_MODE=paper              # 'paper' (default) or 'live'
PAPER_STARTING_BALANCE=100.0    # Starting balance for paper trading simulation

# === KALSHI API ===
# Get your API key from: https://kalshi.com/api
KALSHI_API_KEY_ID=your-api-key-id-here
KALSHI_PRIVATE_KEY_PATH=./kalshi_private_key.pem
# Note: download your PEM private key from the Kalshi dashboard
# and place it at the path specified above

# === POLYMARKET API ===
# IMPORTANT: US persons are restricted from trading on Polymarket per their ToS.
# The bot can read public market data globally. Ensure compliance with local laws.
POLYMARKET_PRIVATE_KEY=your-polygon-wallet-private-key-here
POLYMARKET_FUNDER_ADDRESS=your-funder-wallet-address-here

# === CRYPTO EXCHANGES ===
# Binance market data is free and public (no key needed for read-only)
BINANCE_API_KEY=your-binance-api-key
BINANCE_API_SECRET=your-binance-api-secret
# CoinGecko — free tier available (no key for basic, key increases limits)
COINGECKO_API_KEY=your-coingecko-api-key

# === WEATHER APIs ===
# OpenWeatherMap — free tier: 60 calls/min
# Sign up at: https://openweathermap.org/api
OPENWEATHERMAP_API_KEY=your-openweathermap-key
# WeatherAPI — free tier available
WEATHERAPI_KEY=your-weatherapi-key

# === SPORTS APIs ===
# The Odds API — free tier: 500 requests/month
# Sign up at: https://the-odds-api.com
THE_ODDS_API_KEY=your-the-odds-api-key
# SportsRadar — trial tier available
SPORTSRADAR_API_KEY=your-sportsradar-key

# === NEWS & SENTIMENT ===
# NewsAPI — free tier: 100 requests/day
# Sign up at: https://newsapi.org
NEWS_API_KEY=your-newsapi-key
# Reddit API — register an app at: https://www.reddit.com/prefs/apps
REDDIT_CLIENT_ID=your-reddit-client-id
REDDIT_CLIENT_SECRET=your-reddit-client-secret
REDDIT_USER_AGENT=PredictionBot/1.0 (by /u/your_username)
# Twitter/X API (optional)
TWITTER_BEARER_TOKEN=your-twitter-bearer-token

# === TELEGRAM NOTIFICATIONS ===
# Create a bot via @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_CHAT_ID=your-telegram-chat-id

# === RISK PARAMETERS ===
MAX_POSITIONS=5                 # Maximum concurrent open positions
MAX_EXPOSURE_PER_TRADE=0.20     # Max % of balance per single trade (20%)
MAX_TOTAL_EXPOSURE=0.80         # Max % of balance in all positions combined (80%)
TRADE_THRESHOLD=65              # Minimum composite score to enter a trade (0-100)
STOP_LOSS_PCT=0.15              # Stop loss at 15% below entry price
TAKE_PROFIT_PCT=0.30            # Take profit at 30% above entry price

# === OPTIONAL SETTINGS ===
SENTIMENT_MODEL=vader           # 'vader' (default, fast) or 'finbert' (accurate, slow)
LOG_LEVEL=INFO                  # DEBUG | INFO | WARNING | ERROR
DB_PATH=./trading_bot.db        # SQLite database file path
